# AGENTS.md — curation-research workspace

This is the workspace for the curation-research agent.

## Purpose

Research, curation, synthesis. Find what matters, discard what doesn't.

## Structure

- `reading-lists/` — curated lists by topic
- `research/` — saved outputs, notes, structured findings
- `sources/` — cached articles, papers, references

## Rules

- Always `git pull --rebase` before working in any git repo.
- Write findings to files — don't rely on memory.
- Cite sources with URLs and dates.
